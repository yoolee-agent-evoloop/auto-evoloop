# Synthetic Demo

This demo uses fictional subscription-support conversations. It contains no real user data, credentials, trace exports, endpoints, or private prompts.

Run it with:

```bash
evoloop demo run
```

Generated files are written to `/tmp/auto-evoloop-demo/` unless `--output-dir` is provided.
